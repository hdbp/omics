% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/encode_tsv.R
\name{download_encode_tsv}
\alias{download_encode_tsv}
\title{Download and read a tabular ENCODE file by accession}
\usage{
download_encode_tsv(
  accession,
  dest_dir = ".",
  overwrite = FALSE,
  col_names = FALSE,
  delim = "\\t",
  guess_max = 1000,
  ...
)
}
\arguments{
\item{accession}{Character scalar ENCODE file accession, e.g. \code{"ENCFF000ABC"}.}

\item{dest_dir}{Directory to save the downloaded file. Created if missing. Default \code{"."}.}

\item{overwrite}{Logical; overwrite an existing file of the same name? Default \code{FALSE}.}

\item{col_names}{Passed to \code{readr::read_delim()}; set \code{TRUE} if the file has a header row.
Default \code{FALSE} because many ENCODE files (e.g., BED) lack headers.}

\item{delim}{Field delimiter. Default \code{"\\t"} (tab).}

\item{guess_max}{Max rows to guess column types; forwarded to readr. Default \code{1000}.}

\item{...}{Extra arguments forwarded to \code{readr::read_delim()} (e.g., \code{col_types}, \code{comment}).}
}
\value{
A tibble (data frame) with the parsed contents.
}
\description{
Downloads from
\verb{https://www.encodeproject.org/files/<accession>/@download/}
into \code{dest_dir} (keeping the server-provided filename when available,
e.g. \verb{*.tsv.gz} or \verb{*.bed.gz}), then reads it as a tabular table.
}
\details{
Gzip is detected via the file extension (if provided by the server) or by magic bytes.
\code{readr} can read \code{*.gz} paths directly; no manual connection needed.
}
\examples{
\dontrun{
# Save under ./data and parse as headerless TSV:
df <- download_encode_tsv("ENCFF000ABC", dest_dir = "data", col_names = FALSE)

# If your file has a header row:
df <- download_encode_tsv("ENCFF000XYZ", col_names = TRUE)

# With explicit col_types and skipping commented lines:
df <- download_encode_tsv("ENCFF000XYZ", col_names = TRUE, col_types = readr::cols(), comment = "#")
}

}
\seealso{
\url{https://www.encodeproject.org/help/rest-api/}
}
