% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nrl.R
\name{nrl}
\alias{nrl}
\title{Compute Nucleosome Repeat Length (NRL) and AUC from Fragment Length Distributions}
\usage{
nrl(
  fragments_df,
  sample_id_column = "sample",
  fragment_length_column = "length",
  sample_condition_map = NULL,
  control_condition_name = NULL,
  smoothing_window_size = 5,
  peak_merge_window_bp = 100,
  minima_merge_window_bp = 50,
  arrow_spacing_scale = 1.2,
  condition_colors = NULL,
  ggplot_theme = NULL,
  plot_title_text = "Fragment length distribution with NRL and AUC",
  legend_title_text = "Condition",
  auc_method = c("fixed_window", "peak_to_minimum"),
  auc_window_bp = 100,
  auc_anchor_sample = c("control", "each"),
  show_delta_annotation = TRUE
)
}
\arguments{
\item{fragments_df}{Data frame containing fragment data. Must include fragment
length and sample ID columns.}

\item{sample_id_column}{Character string specifying the column name of sample IDs.}

\item{fragment_length_column}{Character string specifying the column name with fragment lengths (in bp).}

\item{sample_condition_map}{Named character vector mapping sample IDs to conditions.}

\item{control_condition_name}{Character string specifying the name of the control condition.}

\item{smoothing_window_size}{Integer. Rolling mean window size for smoothing fragment distribution.}

\item{peak_merge_window_bp}{Integer. Minimum distance between peaks to be considered distinct (default: 100).}

\item{minima_merge_window_bp}{Integer. Minimum distance between minima to be considered distinct.}

\item{arrow_spacing_scale}{Numeric multiplier controlling vertical spacing between NRL arrows.}

\item{condition_colors}{Named character vector of colors for each condition.}

\item{ggplot_theme}{Optional ggplot2 theme object for plot customization.}

\item{plot_title_text}{Character string specifying the plot title.}

\item{legend_title_text}{Character string specifying the legend title.}

\item{auc_method}{Character; either \code{"fixed_window"} (default) or \code{"peak_to_minimum"}.
Defines how AUC is calculated.}

\item{auc_window_bp}{Integer. If \code{auc_method = "fixed_window"}, defines the ±bp range around peak for AUC integration (default: 100).}

\item{auc_anchor_sample}{Character; \code{"control"} (default) or \code{"each"}, defining
whether the AUC anchor is defined relative to the control sample or each condition independently.}

\item{show_delta_annotation}{Logical; whether to display ΔNRL and ΔAUC annotation in the top-right corner.}
}
\value{
A list with two elements:
\describe{
\item{summary}{A tibble with detected peaks, NRLs, AUC values, and ΔNRL/ΔAUC if control is specified.}
\item{plot}{A ggplot2 object showing smoothed fragment length distribution, NRL arrows, and AUC shading.}
}
}
\description{
Calculates Nucleosome Repeat Length (NRL) and Area Under the Curve (AUC)
from fragment length distributions, optionally comparing control and treatment
conditions with ΔNRL and ΔAUC annotations. Supports both \emph{peak→minimum} and
\emph{fixed-window} AUC calculation modes.
}
